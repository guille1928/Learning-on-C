{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Ejecutar C (sin depurador)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "console": "externalTerminal",
            "preLaunchTask": "compilar C"
        }
    ]
}
